# Theoretical Ablation Experiment

This folder preserves Chengyu Fan's synthetic/theoretical ablation work as a separate experiment track.

Main document:

```text
docs/theoretical_ablation_experiment/synthetic_ablation_report.md
```

This track uses generated scenarios and synthetic labels to evaluate prompt and model behavior. It is useful for controlled prompt/model studies, but it is separate from the Android device-side real eBPF experiment.
